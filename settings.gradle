plugins {
    id 'org.kordamp.gradle.settings' version '0.33.0'
}

rootProject.name = 'JavaBackEnd'

projects {
    layout = 'standard'
    enforceNamingConvention = false
    excludes = ['dev']
}
// Spring boot application
